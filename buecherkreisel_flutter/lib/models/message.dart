import 'user.dart';

class Message {
  DateTime send;
  String message;
  user_id sendBy;
  Message(
      {required this.send,
      required this.message,
      required,
      required this.sendBy});
}
